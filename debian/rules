#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	cargo build --release

override_dh_auto_install:
	install -D -m 755 target/release/json-viewer $(DESTDIR)/usr/bin/json-viewer

override_dh_auto_clean:
	cargo clean

override_dh_auto_test:
	# Skip tests for now
